<%= link_to "All", ideas_path, class: "category category-all" %>

<% Category.all.each do |category| %>
  <%= link_to category.name, ideas_path(category: category.name), 
    class: "category category-#{category.id}" %>
<% end %>
