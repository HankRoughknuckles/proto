<% unless current_user.nil? %>
  <%= link_to "Leave a comment", nil, class: "add_comment" %>

  <% if user_signed_in? %>
    <div>
      <%= form_for [@idea, @comment] do |f| %>
        <%= f.text_field :body %>
        <%= f.submit "Submit" %>
      <% end %>
    </div>
  <% end %>
<% end %>
